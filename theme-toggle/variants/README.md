# Theme toggle structures

The root component uses the single-element gradient structure from `single-structure.html`.
The original nested overlay markup is preserved in `overlay-structure.html` for comparison or future use.
