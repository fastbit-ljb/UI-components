# Theme toggle versions

- `single/`: single circular slider using CSS gradients and animated crater rotation.
- `overlay/`: original nested moon-mask overlay implementation.

Open either folder's `index.html` to preview that version.
